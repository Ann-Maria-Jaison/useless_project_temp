services:
  - type: web
    name: ayyo-playground
    env: python
    buildCommand: apt-get update && apt-get install -y espeak && pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PORT
        value: 10000
